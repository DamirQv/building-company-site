import type { Metadata } from "next";
import Link from "next/link";

export const metadata: Metadata = {
  title: "Проекты домов — Standard Stroy, Алматы",
  description:
    "Типовые проекты домов от Standard Stroy: одноэтажные, двухэтажные, из FinnBlock, газоблока, кирпича. Площадь от 100 до 240 м². Адаптация под ваш участок.",
  alternates: { canonical: "/proekty" },
};

const projects = [
  { title: "Дом 100 м² — одноэтажный", material: "FinnBlock", floors: "1 этаж", area: "100 м²", price: "от 13,5 млн ₸" },
  { title: "Дом 120 м² — одноэтажный", material: "Газоблок", floors: "1 этаж", area: "120 м²", price: "от 18 млн ₸" },
  { title: "Дом 150 м² — двухэтажный", material: "FinnBlock", floors: "2 этажа", area: "150 м²", price: "от 20 млн ₸" },
  { title: "Дом 180 м² — двухэтажный", material: "Газоблок", floors: "2 этажа", area: "180 м²", price: "от 27 млн ₸" },
  { title: "Дом 200 м² — двухэтажный", material: "Кирпич", floors: "2 этажа", area: "200 м²", price: "от 36 млн ₸" },
  { title: "Дом 240 м² — двухэтажный", material: "FinnBlock", floors: "2 этажа", area: "240 м²", price: "от 32 млн ₸" },
];

export default function ProektyPage() {
  return (
    <main className="min-h-screen bg-sky-50 px-6 py-24 text-slate-900 sm:px-8 lg:px-12">
      <div className="mx-auto max-w-6xl">
        <p className="text-sm uppercase tracking-[0.3em] text-sky-600">Проекты</p>
        <h1 className="mt-4 text-4xl font-semibold sm:text-5xl">Типовые проекты домов</h1>
        <p className="mt-6 max-w-3xl text-lg leading-8 text-slate-600">
          Готовые проекты с планировками и сметой. Любой проект можно адаптировать
          под ваш участок и пожелания. Не нашли подходящий — спроектируем с нуля.
        </p>

        <div className="mt-12 grid gap-6 md:grid-cols-2 xl:grid-cols-3">
          {projects.map((proj) => (
            <div key={proj.title} className="rounded-[2rem] border border-slate-200 bg-white p-6 shadow-sm">
              <div className="flex h-40 items-center justify-center rounded-2xl bg-slate-100 text-slate-400">
                <span className="text-sm">Планировка — плейсхолдер</span>
              </div>
              <h2 className="mt-4 text-lg font-semibold text-slate-900">{proj.title}</h2>
              <div className="mt-3 flex flex-wrap gap-2 text-xs text-slate-500">
                <span className="rounded-full bg-slate-100 px-2 py-1">{proj.area}</span>
                <span className="rounded-full bg-slate-100 px-2 py-1">{proj.material}</span>
                <span className="rounded-full bg-slate-100 px-2 py-1">{proj.floors}</span>
              </div>
              <p className="mt-3 text-lg font-semibold text-sky-600">{proj.price}</p>
            </div>
          ))}
        </div>

        <div className="mt-8 rounded-[2rem] border border-slate-200 bg-white p-8 shadow-sm">
          <h2 className="text-2xl font-semibold text-slate-900">Не нашли подходящий проект?</h2>
          <p className="mt-3 text-sm leading-7 text-slate-600">
            Мы проектируем дома с нуля под ваш участок, бюджет и пожелания.
            Стоимость проектирования — от 2 000 ₸/м². Если затем закажете стройку —
            стоимость проекта вычитается из суммы договора.
          </p>
          <div className="mt-4">
            <Link href="/uslugi/proektirovanie" className="inline-flex rounded-full bg-sky-600 px-6 py-3 text-sm font-medium text-white transition hover:bg-sky-700">
              Заказать проект
            </Link>
          </div>
        </div>

        <p className="mt-8 text-xs text-zinc-400">
          * Планировки — плейсхолдеры. Замените на реальные чертежи и рендеры проектов.
        </p>
      </div>
    </main>
  );
}